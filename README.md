# How to Use
Add to project
```pnpm add @ni-tami/minesweeper-core```

From your app:
* Create game module script
* Create UI with element rendering (render grid, event listeners, specified CSS classes)
* Handle localStorage persistence to save and load game/grid state

See [example](./example)
